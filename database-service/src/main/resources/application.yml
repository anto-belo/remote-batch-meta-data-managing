spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/batch_meta_data
    username: root
    password: root
  batch:
    jdbc:
      initialize-schema: always
logging:
  level:
    org.springframework.jdbc: DEBUG

table:
  prefix: BATCH_
